[paths]
python_path = <path to python.exe file>

[devices]
device_id = <reproduction device id> # after portaudio id. Mind the sampling rate, since it must match the chosen signals!

[reproduction]
audio_duration = # audio duration in seconds. Default 1s. Mind the duration of the input signals!
wait_duration = # wait time after each reproduction in second. Default 0s. Note that the real wait time may be longer, due to the  

